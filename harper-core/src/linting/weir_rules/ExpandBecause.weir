set main (cuz)

declare message "Use `because` instead of informal `cuz`"
declare description "Expands the informal abbreviation `cuz` to the full word `because` for formality."
declare kind "Style"
declare becomes "because"

test "Stick around cuz I got a surprise for you at the end." "Stick around because I got a surprise for you at the end."
