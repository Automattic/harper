set main (sooner than later)

declare message "Did you mean `sooner rather than later` or `sooner or later`?"
declare description "Fixes the improper phrase `sooner than later` by suggesting standard alternatives."
declare kind "Usage"
declare becomes ["sooner rather than later", "sooner or later"]

