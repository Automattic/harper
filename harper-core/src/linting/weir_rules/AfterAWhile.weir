set main (after while)

declare message "When describing a timeframe, use `a while`."
declare description "Corrects the missing article in `after while`, forming `after a while`."
declare kind "Grammar"
declare becomes "after a while"

test "bromite Crashes on all sites after while." "bromite Crashes on all sites after a while."
