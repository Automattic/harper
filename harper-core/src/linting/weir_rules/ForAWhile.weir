set main (for while)

declare message "When describing a timeframe, use `a while`."
declare description "Corrects the missing article in `for while`, forming `for a while`."
declare kind "Typo"
declare becomes "for a while"

test "Build flutter releases in github actions for production only android for while." "Build flutter releases in github actions for production only android for a while."
