set main [(half hazard), (half-hazard), (halfhazard)]

declare message "Use `haphazard` for randomness or lack of organization."
declare description "Corrects the eggcorn `half hazard` to `haphazard`, which properly means lacking organization or being random."
declare kind "Eggcorn"
declare becomes "haphazard"

