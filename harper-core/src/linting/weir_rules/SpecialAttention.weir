set main (spacial attention)

declare message "Did you mean `special attention`?"
declare description "Changes `spacial attention` to `special attention`."
declare kind "Typo"
declare becomes "special attention"

test "spacial attention" "special attention"
