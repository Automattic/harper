set main (bare in mind)

declare message "Did you mean `bear in mind`?"
declare description "Ensures the phrase `bear in mind` is used correctly instead of `bare in mind`."
declare kind "Eggcorn"
declare becomes "bear in mind"

