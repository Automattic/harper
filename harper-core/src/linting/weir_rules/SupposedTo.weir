set main (suppose to)

declare message "Did you mean `supposed to`?"
declare description "Fixes `suppose to` to the correct `supposed to`."
declare kind "Usage"
declare becomes "supposed to"

test "suppose to" "supposed to"
