set main (will contains)

declare message "Did you mean `will contain`?"
declare description "Incorrect verb form: `will` should be followed by the base form `contain`."
declare kind "Agreement"
declare becomes "will contain"

