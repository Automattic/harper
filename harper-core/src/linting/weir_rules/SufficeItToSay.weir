set main (suffice to say)

declare message "`Suffice it to say` is the more standard and more common variant."
declare description "Corrects `suffice to say` to `suffice it to say`."
declare kind "Usage"
declare becomes "suffice it to say"

test "I don't fully grok the bug, but suffice to say it is not an RCD issue." "I don't fully grok the bug, but suffice it to say it is not an RCD issue."
