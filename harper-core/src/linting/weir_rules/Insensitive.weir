set main (unsensitive)

declare message "This word has a more standard, more common synonym."
declare description "Suggests the more standard and common synonym `insensitive`."
declare kind "WordChoice"
declare becomes "insensitive"

test "We want to potentially make an unsensitive header" "We want to potentially make an insensitive header"
