set main (same then)

declare message "Did you mean `same as`?"
declare description "Corrects the incorrect phrase `same then` to the standard `same as`."
declare kind "Grammar"
declare becomes "same as"

