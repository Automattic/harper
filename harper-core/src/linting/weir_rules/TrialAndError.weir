set main (trail and error)

declare message "You misspelled `trial`."
declare description "Corrects `trail` to `trial` in `trial and error`."
declare kind "Typo"
declare becomes "trial and error"

test "It was produced through trail and error." "It was produced through trial and error."
