set main (want be)

declare message "Did you mean `won't be` or `want to be`?"
declare description "Detects incorrect usage of `want be` and suggests `won't be` or `want to be` based on context."
declare becomes ["won't be", "want to be"]

