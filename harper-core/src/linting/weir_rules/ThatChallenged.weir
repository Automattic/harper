set main (the challenged)

declare message "Use `that challenged` for correct relative clause."
declare description "Corrects `the challenged` to `that challenged` for proper relative clause usage."
declare becomes "that challenged"

