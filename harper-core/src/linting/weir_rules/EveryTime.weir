set main (everytime)

declare message "`Everytime` as a single word is proscribed. Use `every time` instead."
declare description "Corrects `everytime` to `every time`."
declare kind "Usage"
declare becomes "every time"

test "Init tool everytime a file in a directory is modified" "Init tool every time a file in a directory is modified"
