set main (wellbeing)

declare message "Use the hyphenated form for `well-being`."
declare description "Ensures `well-being` is correctly hyphenated."
declare kind "Punctuation"
declare becomes "well-being"

