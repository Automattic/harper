expr [main capitalises off, main capitalises off of]

let message "Did you mean `capitalises on`?"
let description "Corrects `capitalise off` to `capitalise on`."
let kind "Usage"
let becomes "capitalises on"
