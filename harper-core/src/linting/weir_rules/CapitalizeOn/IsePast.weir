expr main [capitalised off, capitalised off of]

let message "Did you mean `capitalised on`?"
let description "Corrects `capitalise off` to `capitalise on`."
let kind "Usage"
let becomes "capitalised on"
