set main (thanks lot)

declare message "The indefinite article `a` is required in `thanks a lot`."
declare description "Corrects the missing article in `thanks lot`, forming `thanks a lot`."
declare kind "Grammar"
declare becomes "thanks a lot"

test "thanks lot" "thanks a lot"
test "thanks a lot" "thanks a lot"
